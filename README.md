# もぐもぐ、いつから？ (babyfood)

離乳食の記録をステージ（月齢）ごとに写真とメモで残すFlutterアプリ。

## 主な機能

- ステージ（ゴックン期・モグモグ期・カミカミ期・パクパク期）と月齢で記録をフィルタ
- カメラ撮影 / アルバムから写真を選んで記録を追加（自動で縮小・圧縮）
- 記録は端末内にローカル保存（アプリのドキュメントディレクトリ）

## 開発

```
flutter pub get
flutter run
```
